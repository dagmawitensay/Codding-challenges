# ccwc - Command-Line Word Counter

`ccwc` is a simple command-line tool to count lines, words, characters, and bytes from a file or piped input.

---

## Usage

### Flags
- `-l` : Count lines.
- `-w` : Count words.
- `-m` : Count characters.
- `-c` : Count bytes.

---